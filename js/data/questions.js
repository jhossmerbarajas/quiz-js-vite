// import { data } from './data'
// import { Question } from '../models/Question'

// export const questions = data.map(
// 	(question) => Question(question.question, question.answer, question.options) 
// )
